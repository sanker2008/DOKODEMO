# DokoDemo review 修复与验收记录

日期：2026-09-17。基线：main@1a55feb。修复分支：codex/review-fixes。

用户要求修复前轮代码与 UI/UX 审查全部问题，并重点检查扫码添加节点；同时明确要求不在本地打包构建。本轮没有运行 Gradle、编译、打包、JVM 测试、模拟器或真机测试。下表“已实现”仅表示源码修复已落地，不表示运行验收已完成。此前审查旧版本的构建及模拟器截图不能作为本轮修复的运行证据。

## 代码修复

| 原问题 | 源码修复 | 状态 |
| --- | --- | --- |
| Release 使用公开 debug 签名 | release 仅使用独立签名配置；缺少凭据时打包失败；CI 从 secrets 加载临时密钥，先测试/lint；手动运行只上传制品，标签才发布 | 已实现；正式密钥待配置 |
| 空/HTML/不支持订阅清空旧节点，替换非事务 | 拒绝零有效节点，关闭响应，限制响应大小；统一同步入口串行更新，分组/节点/quota 同一 Room 事务；更新前重查订阅存在及 URL，取消传播 | 已实现 |
| VPN 启停协程失控 | service scope 持有启动任务；进程级串行线程执行原生启停；取消/销毁释放核心和 TUN；按 startId 停止，防止旧请求停止新连接 | 已实现 |
| 允许 LAN 开关状态未同步 | 将第三个 preference 合并到 UI state | 已实现 |
| 编辑节点丢元数据 | 基于原记录 copy；事务保存时保留最新订阅、选择、位置、历史和延迟元数据；已删除节点不会被复活 | 已实现 |
| Shadowsocks 加密字段错用 | 出站生成使用 ssMethod，导入及编辑回填同一字段 | 已实现 |
| 缺少 Shadowsocks 分享导入 | 支持 SIP002、URL-safe Base64、旧版完整 Base64、IPv6、密码冒号和保留字符；不支持的 SIP003 插件明确拒绝 | 已实现 |
| Trojan 密码编辑无效 | 输入框绑定 password；旧 uuid 密码在载入与连接边界兼容 | 已实现 |
| IPv6 链接解析失败 | 使用 URI authority/host 解析方括号 IPv6，校验端口；避免整条链接重复解码 | 已实现 |
| 分应用空选择变全局、allow/disallow 混用 | 仅代理模式必须至少成功添加一个已安装应用；同一 Builder 只用一种列表；绕过模式可空选全局 | 已实现 |
| Reality 自动强加 Vision | flow 只使用用户配置值；不自动补 Vision | 已实现 |

其他相关修复：原生隧道失败不再假报成功；通知速度刷新保留断开动作；native cache 使用实际 Context.cacheDir；停止清除运行状态；未实现恢复时不再声明 always-on；TCP socket 使用 use 关闭；快捷设置连接异常恢复入口；移除无效的 testConfig 空实现和重复 policy；取消无意义自动轮换同一节点。

## UI/UX 修复

| 审查编号 | 对应修复 | 状态 |
| --- | --- | --- |
| U01 | 首页可滚动，连接主区优先，横屏可继续滚动触达操作 | 已实现；设备验收待进行 |
| U02 | 浅色正文/标题/错误/按钮对比度提升；保留蓝灰与薄荷色；分组标签与延迟颜色同步修正 | 已实现；调色板计算通过 |
| U03 | 拒绝权限时显示说明、授权/设置/相册入口；恢复前台重查权限；无相机或闪光灯时不假装扫描/可照明 | 已实现；设备验收待进行 |
| U04 | 地址、端口、UUID/密码、Reality 校验；保存防重复；连接错误常驻，提供节点入口和重试；连接中可取消 | 已实现 |
| U05 | 空态和节点/连接操作在图表前；未连接时隐藏空图表 | 已实现 |
| U06 | 节点添加菜单包含订阅入口；新增订阅立即拉取，展示数量及返回节点列表提示；扫码保存原子新增并选中 | 已实现 |
| U07 | 订阅文案明确支持 Base64/逐行 VMess/VLESS/Trojan/SS，不支持 Clash YAML；不支持内容保留旧节点 | 已实现 |
| U08 | 补齐英文/繁中资源、订阅页/主要反馈与辅助描述本地化；路由枚举界面本地化；API 24–32 语言持久化元数据 | 已实现；资源一致性通过 |
| U09 | 节点、订阅、清空流量历史增加确认并说明范围；删除订阅事务解除关联、保留节点和分组 | 已实现 |
| U10 | 设置与分应用页说明下次连接生效；分应用页提供启用开关与空选语义说明 | 已实现 |
| U11 | 深色历史说明改正文语义色；节点/首页菜单使用表面色 | 已实现 |
| U12 | 路由输入框圆角与其他表单一致，弹窗可滚动，弱化低对比 placeholder；日志使用主题色，提供复制/分享与说明；同数量新日志可刷新 | 已实现 |
| U13 | 记录真实下载量级，动态缩放，显示最近 50 秒与峰值，不再截断 1 MiB/s 以上值 | 已实现；真实流量验收待进行 |
| U14 | 首页和表单限制平板内容宽度；首页/扫码/分应用/表单弹窗操作可滚动触达；字体缩放同步行高 | 已实现；设备验收待进行 |
| U15 | 实際列表派生包含 sortOption，名称排序与延迟排序生效，未测/失败节点排后 | 已实现 |

## 扫码专项修复

- 相机 use cases 仅随页面生命周期绑定，退出时解绑自身 use cases、清空 analyzer 并关闭线程。
- 分析结果通过主线程接受；有效节点才进入编辑器；相机和相册共享一次性接受逻辑，避免多次跳转。
- 正确处理 Y plane 的行填充、像素步长、0/90/180/270 度旋转；允许最后一行没有尾部 padding。
- 支持普通/反色 QR 尝试；不再将二维码开头的凭据写入日志。
- 相册解码在 IO 线程执行，先读取尺寸再采样，限制最长边约 2048 像素，关闭输入流并回收 Bitmap。
- 导航用 Android Uri.encode，解析字段时只解码一次，保留字面加号、百分号、中文、嵌套 query 与密码中的冒号。
- 回填包含 SS method、KCP seed、gRPC serviceName 等字段；屏幕重建不再重复回填覆盖用户已编辑的值。
- 保存与选中同一事务，重复点击不会重复创建；输入或存储错误保留在编辑页面。

## 静态验证证据

结果：`.codex-review/fixes/static-checks.json`。复核脚本：`.codex-review/fixes/static_checks.py`（需要 Python tree-sitter、tree-sitter-kotlin；只解析，不编译）。

- 73 个 Kotlin/KTS 文件语法解析，无错误。该检查不做 Kotlin 类型解析，也不替代编译。
- 12 个 XML 文件可解析；字符串引用无缺失，三个语言目录无重复键、缺失翻译键或格式占位符不一致。
- `git diff --check` 通过。
- 新增 43 项 JVM 回归用例：分享链接 15、出站生成 4、订阅抓取 4、扫码亮度处理 8、配置校验 12。只写好源码，未执行；无 RED/GREEN 或测试通过声明。
- 源码调色板对比度：浅色标题 6.78:1、正文 5.35:1、主色文字 6.18:1、白字主按钮 6.83:1、错误文字 5.28:1、选中分组标签 5.47:1。不是修改后设备截图或实际渲染测量。

## 发布前运行验收清单（均未执行）

1. 在 CI/获准环境执行 `testDebugUnitTest lintDebug`，解决所有编译、单测和 lint 错误后才打包。
2. ARM 真机上分别扫描真实 VMess/VLESS/Trojan/SS 可用节点，确认仅打开一个编辑器、字段不丢失、保存后节点选中、重启应用记录仍在。
3. 扫描含中文名称、+、%、#、@、冒号、IPv6 的分享码；普通二维码、损坏内容应留在扫描页提示，随后仍能识别有效码。
4. 首次拒绝、再次拒绝、永久拒绝后去设置授权、权限被系统撤销、无后置相机/无闪光灯、前后台切换、旋转屏幕、快速返回重进均检查。
5. 相册取消、不含二维码、反色二维码、大图片、撤回读取权限；解析时返回不应跳转到其他页面。
6. 编辑回填后旋转屏幕，输入保持；快速双击保存只有一条；编辑已有订阅节点保持订阅、选中和历史；并发删除不复活。
7. 有效/空/HTML/Clash/超大/超时订阅刷新；失败保留旧节点；并发更新、修改 URL、删除订阅不产生孤立组或已删除订阅节点。
8. 使用真实节点验证 TCP、WebSocket、gRPC、TLS、Reality 空 flow/显式 Vision、SS 各支持 method、KCP；检查 DNS、UDP、IPv4/IPv6 与原生库行为。源码不能证明远端可达。
9. 快速连接/取消/重连、切节点、撤销 VPN 权限、停止服务，确保无幽灵连接、TUN 泄漏或通知残留；通知始终可断开。
10. 分应用仅代理空集/全部卸载应失败；部分有效应用只代理有效项；绕过模式正常；LAN 开关双向持久化并重新连接验证监听范围。
11. 浅/深色、简/繁/英、360 dp 小屏、640×360 dp 横屏、平板、系统 200% 字号加应用 Large，检查每个关键按钮与输入可达；TalkBack 检查扫码、连接、错误提示。
12. 节点排序/搜索/分组、所有删除确认、历史清空、日志刷新/复制/分享、订阅导入数量和选中流程逐项验收。
13. 真实流量超过 1 MiB/s 时检查图表动态量级、速度与历史累计；原生 queryStats 的累计/重置语义必须以所集成库的实际表现确认。
14. 配置正式签名 secrets 后验证 release 证书与安装升级策略，参见 RELEASE_SIGNING.md。公开 debug 密钥安装包不能直接被不同密钥覆盖升级。

修复记录生成时尚未运行 CI。本次按用户要求随 v1.0.2 推送并触发 Actions；运行结果以 GitHub Actions 为准。本记录不声称所有功能已通过运行验收。
